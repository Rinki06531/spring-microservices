eureka:
  client:
    serviceUrl:
      defaultZone: http://192.168.43.236:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}
    hostname: 192.168.43.236
server:
  port: ${PORT:0}
spring:
  cloud:
    client:
      hostname: 192.168.43.236
  application:
    name: users-ws
  devtools:
    restart:
      enabled: true